# Image

## Possible Usage

- Use Image to display images in the UI, such as logos or product photos.
- Example:
  ```php
  // Usage example will be filled in with more context if available
  ```
# HtmlEditor

## Possible Usage

- Use HtmlEditor to provide a rich text or HTML editing interface for users.
- Example:
  ```php
  // Usage example will be filled in with more context if available
  ```

