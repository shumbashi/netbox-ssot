# Modal

## Possible Usage

- Use Modal to display content in a modal dialog overlaying the main UI.
- Example:
  ```php
  // Usage example will be filled in with more context if available
  ```
# ListInfo

## Possible Usage

- Use ListInfo to display informational lists or grouped data in the UI.
- Example:
  ```php
  // Usage example will be filled in with more context if available
  ```

