# RadioButton

## Possible Usage

- Use RadioButton to allow users to select a single option from a set of choices.
- Example:
  ```php
  // Usage example will be filled in with more context if available
  ```

