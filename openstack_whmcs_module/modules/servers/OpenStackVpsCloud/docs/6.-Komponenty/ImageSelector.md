# ImageSelector

## Possible Usage

- Use ImageSelector to provide a UI for selecting images from a predefined set.
- Example:
  ```php
  // Usage example will be filled in with more context if available
  ```

