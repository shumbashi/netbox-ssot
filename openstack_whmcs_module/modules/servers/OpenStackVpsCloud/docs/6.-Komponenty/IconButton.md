# IconButton

## Possible Usage

- Use IconButton to display a button with an icon, often for quick actions.
- Example:
  ```php
  // Usage example will be filled in with more context if available
  ```

