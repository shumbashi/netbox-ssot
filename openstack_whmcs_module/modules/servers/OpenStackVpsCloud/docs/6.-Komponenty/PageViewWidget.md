# PageViewWidget

## Possible Usage

- Use PageViewWidget to display a widget or summary related to the current page view.
- Example:
  ```php
  // Usage example will be filled in with more context if available
  ```
# Number

## Possible Usage

- Use Number to display or input numeric values in the UI.
- Example:
  ```php
  // Usage example will be filled in with more context if available
  ```

