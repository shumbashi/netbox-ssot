# TicketReplyPreview

## Possible Usage

- Use TicketReplyPreview to display a preview of a ticket reply before submission.
- Example:
  ```php
  // Usage example will be filled in with more context if available
  ```
# Text

## Possible Usage

- Use Text to display plain or formatted text in the UI.
- Example:
  ```php
  // Usage example will be filled in with more context if available
  ```

