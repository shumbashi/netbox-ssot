# CopyPasswordInline

## Possible Usage

- Use CopyPasswordInline to display a password with a copy-to-clipboard button inline.
- Example:
  ```php
  // Usage example will be filled in with more context if available
  ```
# Checkbox

## Possible Usage

- Use Checkbox to allow users to select one or more options from a set.
- Example:
  ```php
  // Usage example will be filled in with more context if available
  ```

